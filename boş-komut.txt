const Discord = require("discord.js")
const db = require("justdata");
const { messageLink } = require("discord.js");
module.exports = {
    name: "komut ismi",
    description: "komut açiklamasi",
    type: 1,
    options: [],
    run: async (client, interaction) => {
    
    
    
    }
}
